# Postgres connection string — use Neon URL for dev/prod, Docker URL for local testing
DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require

# HTTP server port (optional, defaults to 8080)
PORT=8080