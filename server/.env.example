# .env: Neon Postgres for shared/prod-like runs
# .env.dev: Local Docker Postgres for npm run dev
# .env.test: Local Docker Postgres for tests
DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require

# HTTP server port (optional, defaults to 8080)
PORT=8080